"use client";

import { ChangeEvent, FormEvent, ReactNode, useEffect, useMemo, useRef, useState } from "react";
import "./os.css";

type Kind = "client" | "project" | "file" | "account" | "transaction";
type AccountingType = "customer-payment" | "other-income" | "expense";
type PaymentStage = "sample" | "advance" | "progress" | "final" | "other";
type RecordItem = {
  id: string;
  kind: Kind;
  title: string;
  createdAt: string;
  company?: string;
  email?: string;
  phone?: string;
  notes?: string;
  status?: string;
  role?: string;
  budget?: number;
  progress?: number;
  deadline?: string;
  clientId?: string;
  projectId?: string;
  fileName?: string;
  dataUrl?: string;
  mimeType?: string;
  size?: number;
  amount?: number;
  transactionType?: "income" | "expense";
  accountingType?: AccountingType;
  paymentStage?: PaymentStage;
  paymentMethod?: string;
  category?: string;
  date?: string;
};
type Store = { records: RecordItem[]; activities: string[]; notifications: string[] };
type View = "Overview" | "Clients" | "Projects" | "Files" | "Accounts" | "Finances" | "Settings";

const emptyStore: Store = { records: [], activities: [], notifications: [] };
const views: { key: View; icon: string; label: string }[] = [
  { key: "Overview", icon: "⌂", label: "Overview" },
  { key: "Clients", icon: "◉", label: "Clients" },
  { key: "Projects", icon: "◇", label: "Projects" },
  { key: "Files", icon: "⌁", label: "Files & pictures" },
  { key: "Accounts", icon: "◎", label: "Accounts" },
  { key: "Finances", icon: "৳", label: "Finances" },
];
const statuses = ["Planning", "Active", "On hold", "Complete"];
const paymentStages: { label: string; value: PaymentStage }[] = [
  { label: "Sample payment", value: "sample" },
  { label: "Advance payment", value: "advance" },
  { label: "Progress payment", value: "progress" },
  { label: "Final payment", value: "final" },
  { label: "Other payment", value: "other" },
];

const makeId = (prefix: string) => `${prefix}-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 7)}`;
const money = (value: number) => `৳${new Intl.NumberFormat("en-BD", { maximumFractionDigits: 0 }).format(value || 0)}`;
const dateLabel = (value: string) => new Intl.DateTimeFormat("en-BD", { day: "numeric", month: "short", year: "numeric" }).format(new Date(value));
const bytes = (value = 0) => value < 1024 * 1024 ? `${Math.max(1, Math.round(value / 1024))} KB` : `${(value / 1024 / 1024).toFixed(1)} MB`;
const isPicture = (item: RecordItem) => Boolean(item.dataUrl && (item.mimeType?.startsWith("image/") || /\.(avif|gif|jpe?g|png|svg|webp)$/i.test(item.fileName || item.title)));
const isCustomerPayment = (item: RecordItem) => item.transactionType === "income" && (item.accountingType === "customer-payment" || (!item.accountingType && Boolean(item.projectId)));
const projectReceived = (projectId: string, transactions: RecordItem[]) => transactions.filter((item) => item.projectId === projectId && isCustomerPayment(item)).reduce((sum, item) => sum + (Number(item.amount) || 0), 0);
const projectBalance = (project: RecordItem, transactions: RecordItem[]) => Math.max(0, (Number(project.budget) || 0) - projectReceived(project.id, transactions));

function normalise(input: Partial<Store>): Store {
  const records = Array.isArray(input.records) ? input.records.filter((record): record is RecordItem => Boolean(record && (record as RecordItem).kind && (record as RecordItem).title)) : [];
  return {
    records,
    activities: records.length && Array.isArray(input.activities) ? input.activities.filter((item): item is string => typeof item === "string") : [],
    notifications: records.length && Array.isArray(input.notifications) ? input.notifications.filter((item): item is string => typeof item === "string") : [],
  };
}

export default function AphurantaOS() {
  const [store, setStore] = useState<Store>(emptyStore);
  const [view, setView] = useState<View>("Overview");
  const [search, setSearch] = useState("");
  const [modal, setModal] = useState<Kind | null>(null);
  const [modalProjectId, setModalProjectId] = useState<string | undefined>();
  const [modalClientId, setModalClientId] = useState<string | undefined>();
  const [editingId, setEditingId] = useState<string | null>(null);
  const [selectedId, setSelectedId] = useState<string | null>(null);
  const [previewId, setPreviewId] = useState<string | null>(null);
  const [toast, setToast] = useState("");
  const loaded = useRef(false);

  useEffect(() => {
    let mounted = true;
    fetch("/api/store")
      .then((response) => response.ok ? response.json() : Promise.reject(new Error("Unable to load workspace")))
      .then((data: Partial<Store>) => { if (mounted) setStore(normalise(data)); })
      .catch(() => {})
      .finally(() => { loaded.current = true; });
    return () => { mounted = false; };
  }, []);

  useEffect(() => {
    if (!loaded.current) return;
    const timer = window.setTimeout(() => {
      fetch("/api/store", { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify(store) }).catch(() => {});
    }, 350);
    return () => window.clearTimeout(timer);
  }, [store]);

  const clients = useMemo(() => store.records.filter((item) => item.kind === "client"), [store.records]);
  const projects = useMemo(() => store.records.filter((item) => item.kind === "project"), [store.records]);
  const files = useMemo(() => store.records.filter((item) => item.kind === "file"), [store.records]);
  const accounts = useMemo(() => store.records.filter((item) => item.kind === "account"), [store.records]);
  const transactions = useMemo(() => store.records.filter((item) => item.kind === "transaction"), [store.records]);
  const query = search.trim().toLowerCase();
  const filtered = useMemo(() => store.records.filter((item) => !query || Object.values(item).join(" ").toLowerCase().includes(query)), [query, store.records]);
  const metrics = {
    clients: clients.length,
    projects: projects.length,
    activeProjects: projects.filter((item) => item.status === "Active").length,
    budget: projects.reduce((sum, item) => sum + (Number(item.budget) || 0), 0),
    income: transactions.filter((item) => item.transactionType === "income").reduce((sum, item) => sum + (Number(item.amount) || 0), 0),
    expenses: transactions.filter((item) => item.transactionType === "expense").reduce((sum, item) => sum + (Number(item.amount) || 0), 0),
    collected: transactions.filter(isCustomerPayment).reduce((sum, item) => sum + (Number(item.amount) || 0), 0),
    receivable: projects.reduce((sum, item) => sum + projectBalance(item, transactions), 0),
  };

  const notify = (message: string) => {
    setToast(message);
    window.setTimeout(() => setToast(""), 2400);
  };
  const open = (next: View) => { setView(next); setSearch(""); setSelectedId(null); };
  const openRecord = (item: RecordItem) => { setSelectedId(item.id); setSearch(""); };
  const startCreate = (kind: Kind, projectId?: string, clientId?: string) => { setEditingId(null); setModalProjectId(projectId); setModalClientId(clientId); setModal(kind); };
  const startEdit = (item: RecordItem) => { setEditingId(item.id); setModalProjectId(item.projectId); setModalClientId(item.clientId); setModal(item.kind); };
  const closeModal = () => { setModal(null); setModalProjectId(undefined); setModalClientId(undefined); setEditingId(null); };
  const saveRecord = (record: RecordItem) => {
    if (editingId) {
      setStore((current) => ({ ...current, records: current.records.map((item) => item.id === editingId ? record : item), activities: [`${record.title} updated`, ...current.activities].slice(0, 30) }));
      notify(`${record.title} updated`);
    } else {
      setStore((current) => ({ ...current, records: [record, ...current.records], activities: [`${record.title} added`, ...current.activities].slice(0, 30) }));
      notify(`${record.title} added`);
    }
    closeModal();
  };
  const removeRecord = (id: string) => {
    const item = store.records.find((record) => record.id === id);
    if (!item) return;
    setStore((current) => ({ ...current, records: current.records.filter((record) => record.id !== id), activities: [`${item.title} removed`, ...current.activities].slice(0, 30) }));
    if (selectedId === id) setSelectedId(null);
    notify("Removed");
  };
  const clientName = (id?: string) => clients.find((client) => client.id === id)?.title || "Unassigned";
  const projectName = (id?: string) => projects.find((project) => project.id === id)?.title || "Unassigned";
  const selected = store.records.find((item) => item.id === selectedId) || null;
  const previewFile = files.find((item) => item.id === previewId) || null;

  return (
    <main className="os-shell">
      <aside className="os-sidebar">
        <div className="brand"><div className="brand-mark">A</div><div><strong>APHURANTA</strong><small>APPAREL OPERATIONS</small></div></div>
        <nav className="nav-list">
          {views.map((item) => <button key={item.key} className={view === item.key ? "nav-item active" : "nav-item"} onClick={() => open(item.key)}><span>{item.icon}</span>{item.label}{item.key === "Projects" && metrics.activeProjects > 0 ? <b>{metrics.activeProjects}</b> : null}</button>)}
        </nav>
        <div className="sidebar-bottom"><button className={view === "Settings" ? "nav-item active" : "nav-item"} onClick={() => open("Settings")}><span>⚙</span>Settings</button><div className="signed-in"><i>JS</i><div><strong>Jahid S.</strong><small>Administrator</small></div></div></div>
      </aside>

      <section className="os-work">
        <header className="os-top"><div className="mobile-brand"><div className="brand-mark">A</div><strong>APHURANTA</strong></div><label className="search-box"><span>⌕</span><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search clients, projects, files…" /><kbd>⌘ K</kbd></label><button className="primary top-add" onClick={() => startCreate("client")}>＋ <span>Add new</span></button><div className="top-avatar">JS</div></header>
        {selected ? <RecordDetailV2 item={selected} clients={clients} projects={projects} files={files} transactions={transactions} clientName={clientName} projectName={projectName} onBack={() => setSelectedId(null)} onSelect={openRecord} onEdit={startEdit} onPreview={setPreviewId} onAddFile={(projectId) => startCreate("file", projectId)} onAddProject={(clientId) => startCreate("project", undefined, clientId)} /> : <>
          {view === "Overview" ? <Overview metrics={metrics} clients={clients} projects={projects} files={files} accounts={accounts} activities={store.activities} open={open} create={startCreate} /> : null}
          {view === "Finances" ? <FinancePage transactions={transactions} projects={projects} clients={clients} create={() => startCreate("transaction")} edit={startEdit} remove={removeRecord} /> : null}
          {view === "Settings" ? <Settings /> : null}
          {view === "Clients" ? <ListPage title="Clients" description="Keep every client profile in one clear place." actionLabel="New client" onAction={() => startCreate("client")} onSelect={openRecord} rows={filtered.filter((item) => item.kind === "client")} empty="No client profiles yet. Add your first client to begin." render={(item) => <div className="row-main"><div className="avatar">{item.title.slice(0, 1).toUpperCase()}</div><div><strong>{item.title}</strong><small>{item.company || item.email || "No company details"}</small></div></div>} right={(item) => <div className="row-right"><span>{item.phone || item.email || "—"}</span><button className="text-button" onClick={(event) => { event.stopPropagation(); startEdit(item); }}>Edit</button><button className="text-button danger" onClick={(event) => { event.stopPropagation(); removeRecord(item.id); }}>Remove</button></div>} /> : null}
          {view === "Projects" ? <ListPage title="Projects" description="Track work, order value, delivery progress, and payment balance together." actionLabel="New project" onAction={() => startCreate("project")} onSelect={openRecord} rows={filtered.filter((item) => item.kind === "project")} empty="No projects yet. Create one from a client brief." render={(item) => <div className="row-main"><div className="project-dot">◇</div><div><strong>{item.title}</strong><small>{clientName(item.clientId)}{item.deadline ? ` · Due ${dateLabel(item.deadline)}` : ""} · {item.progress || 0}% complete</small></div></div>} right={(item) => <div className="row-right"><span className={`status ${item.status?.toLowerCase().replaceAll(" ", "-")}`}>{item.status || "Planning"}</span><span>{money(item.budget || 0)}</span><button className="text-button" onClick={(event) => { event.stopPropagation(); startEdit(item); }}>Edit</button><button className="text-button danger" onClick={(event) => { event.stopPropagation(); removeRecord(item.id); }}>Remove</button></div>} /> : null}
          {view === "Files" ? <ListPage title="Files & pictures" description="Attach references, tech packs, and images to the right project." actionLabel="Add file or picture" onAction={() => startCreate("file")} onSelect={openRecord} rows={filtered.filter((item) => item.kind === "file")} empty="No files yet. Add a picture or document to your workspace." render={(item) => <div className="row-main">{isPicture(item) ? <img className="file-thumb" src={item.dataUrl} alt={item.fileName || item.title} /> : <div className="file-icon">▤</div>}<div><strong>{item.fileName || item.title}</strong><small>{projectName(item.projectId)} · {bytes(item.size)}</small></div></div>} right={(item) => <div className="row-right"><button className="text-button" onClick={(event) => { event.stopPropagation(); setPreviewId(item.id); }}>Preview</button>{item.dataUrl ? <a className="text-button" href={item.dataUrl} download={item.fileName || item.title} onClick={(event) => event.stopPropagation()}>Download</a> : null}<button className="text-button danger" onClick={(event) => { event.stopPropagation(); removeRecord(item.id); }}>Remove</button></div>} /> : null}
          {view === "Accounts" ? <ListPage title="Accounts" description="Manage the people who can work in this workspace." actionLabel="Add account" onAction={() => startCreate("account")} onSelect={openRecord} rows={filtered.filter((item) => item.kind === "account")} empty="No team accounts yet. Add the people who need access." render={(item) => <div className="row-main"><div className="avatar dark">{item.title.slice(0, 1).toUpperCase()}</div><div><strong>{item.title}</strong><small>{item.email || "No email added"}</small></div></div>} right={(item) => <div className="row-right"><span>{item.role || "Member"}</span><span className={`status ${item.status?.toLowerCase()}`}>{item.status || "Active"}</span><button className="text-button" onClick={(event) => { event.stopPropagation(); startEdit(item); }}>Edit</button><button className="text-button danger" onClick={(event) => { event.stopPropagation(); removeRecord(item.id); }}>Remove</button></div>} /> : null}
        </>}
      </section>
      {modal ? <CreateModalV2 key={`${modal}-${editingId || "new"}`} kind={modal} initial={editingId ? store.records.find((item) => item.id === editingId) : undefined} clients={clients} projects={projects} initialProjectId={modalProjectId} initialClientId={modalClientId} onClose={closeModal} onSave={saveRecord} /> : null}
      {previewFile ? <PreviewModal file={previewFile} projectName={projectName} onClose={() => setPreviewId(null)} /> : null}
      {toast ? <div className="toast">{toast}</div> : null}
    </main>
  );
}

function Overview({ metrics, clients, projects, files, accounts, activities, open, create }: { metrics: { clients: number; projects: number; activeProjects: number; budget: number; collected: number; receivable: number }; clients: RecordItem[]; projects: RecordItem[]; files: RecordItem[]; accounts: RecordItem[]; activities: string[]; open: (view: View) => void; create: (kind: Kind) => void }) {
  return <div className="page"><div className="page-heading"><div><p className="eyebrow">Workspace overview</p><h1>Good morning, Jahid.</h1><p className="muted">A simple view of what is actually saved in Aphuranta.</p></div><button className="primary" onClick={() => create("client")}>＋ Add something</button></div><div className="metric-grid"><Metric label="Clients" value={String(metrics.clients)} hint="Saved profiles" /><Metric label="Projects" value={String(metrics.projects)} hint={`${metrics.activeProjects} active`} /><Metric label="Order book" value={money(metrics.budget)} hint="Total project value" dark /><Metric label="Collected" value={money(metrics.collected)} hint="Customer payments" /><Metric label="Balance due" value={money(metrics.receivable)} hint="Open project balances" /><Metric label="Files & pictures" value={String(files.length)} hint="Attached workspace files" /></div><div className="overview-grid"><section className="panel quick-panel"><div className="panel-heading"><div><h2>Start here</h2><p className="muted">Choose the next thing you want to add.</p></div></div><div className="quick-actions"><button onClick={() => create("client")}><span>◉</span><strong>Client profile</strong><small>Name, contact, notes</small></button><button onClick={() => create("project")}><span>◇</span><strong>Project / order</strong><small>Value, delivery, progress</small></button><button onClick={() => create("transaction")}><span>৳</span><strong>Payment or expense</strong><small>Sample, advance, balance</small></button><button onClick={() => create("file")}><span>⌁</span><strong>File or picture</strong><small>Reference or tech pack</small></button></div></section><section className="panel"><div className="panel-heading"><div><h2>Recent activity</h2><p className="muted">Your latest changes</p></div></div>{activities.length ? <div className="activity-list">{activities.slice(0, 6).map((item, index) => <div className="activity" key={`${item}-${index}`}><i>•</i><div><strong>{item}</strong><small>Saved to workspace</small></div></div>)}</div> : <Empty title="Nothing saved yet" text="New records will appear here." />}</section></div><section className="panel snapshot"><div className="panel-heading"><div><h2>Workspace snapshot</h2><p className="muted">Everything currently stored</p></div></div><div className="snapshot-grid"><button onClick={() => open("Clients")}><strong>{clients.length}</strong><span>clients</span><em>View clients →</em></button><button onClick={() => open("Projects")}><strong>{projects.length}</strong><span>projects</span><em>View projects →</em></button><button onClick={() => open("Files")}><strong>{files.length}</strong><span>files & pictures</span><em>View files →</em></button><button onClick={() => open("Finances")}><strong>{money(metrics.receivable)}</strong><span>balance due</span><em>Open finances →</em></button></div></section></div>;
}

function Metric({ label, value, hint, dark = false }: { label: string; value: string; hint: string; dark?: boolean }) { return <div className={dark ? "metric dark" : "metric"}><small>{label}</small><strong>{value}</strong><span>{hint}</span></div>; }
function Empty({ title, text }: { title: string; text: string }) { return <div className="empty"><strong>{title}</strong><span>{text}</span></div>; }
function ListPage({ title, description, actionLabel, onAction, onSelect, rows, empty, render, right }: { title: string; description: string; actionLabel: string; onAction: () => void; onSelect: (item: RecordItem) => void; rows: RecordItem[]; empty: string; render: (item: RecordItem) => ReactNode; right: (item: RecordItem) => ReactNode }) { return <div className="page"><div className="page-heading"><div><p className="eyebrow">Workspace</p><h1>{title}</h1><p className="muted">{description}</p></div><button className="primary" onClick={onAction}>＋ {actionLabel}</button></div><section className="panel list-panel"><div className="list-count">{rows.length} {rows.length === 1 ? "record" : "records"} <span className="list-hint">Select a row to see connected details</span></div>{rows.length ? <div className="rows">{rows.map((item) => <div className="data-row clickable" key={item.id} onClick={() => onSelect(item)}>{render(item)}{right(item)}</div>)}</div> : <Empty title="Nothing here yet" text={empty} />}</section></div>; }

function FinancePage({ transactions, projects, clients, create, edit, remove }: { transactions: RecordItem[]; projects: RecordItem[]; clients: RecordItem[]; create: () => void; edit: (item: RecordItem) => void; remove: (id: string) => void }) {
  const income = transactions.filter((item) => item.transactionType === "income").reduce((sum, item) => sum + (Number(item.amount) || 0), 0);
  const expenses = transactions.filter((item) => item.transactionType === "expense").reduce((sum, item) => sum + (Number(item.amount) || 0), 0);
  const collected = transactions.filter(isCustomerPayment).reduce((sum, item) => sum + (Number(item.amount) || 0), 0);
  const orderBook = projects.reduce((sum, item) => sum + (Number(item.budget) || 0), 0);
  const receivable = projects.reduce((sum, item) => sum + projectBalance(item, transactions), 0);
  const net = income - expenses;
  const clientName = (id?: string) => clients.find((client) => client.id === id)?.title || "No client";
  const projectName = (id?: string) => projects.find((project) => project.id === id)?.title || "No project";
  const stageLabel = (item: RecordItem) => item.transactionType === "expense" ? (item.category || "Expense") : item.accountingType === "other-income" ? "Other income" : `${paymentStages.find((stage) => stage.value === item.paymentStage)?.label || "Customer payment"}`;
  return <div className="page"><div className="page-heading"><div><p className="eyebrow">Order-to-cash ledger</p><h1>Finances</h1><p className="muted">See what was ordered, what the client has paid, what the business spent, and what remains due.</p></div><button className="primary" onClick={create}>＋ Add payment or expense</button></div><div className="finance-summary"><div className="finance-card"><small>Order book</small><strong>{money(orderBook)}</strong><span>All project values</span></div><div className="finance-card positive"><small>Collected</small><strong>{money(collected)}</strong><span>Customer payments</span></div><div className="finance-card negative"><small>Balance due</small><strong>{money(receivable)}</strong><span>Open project balances</span></div><div className="finance-card"><small>Net cash</small><strong>{money(net)}</strong><span>{money(income)} in · {money(expenses)} out</span></div></div><section className="panel list-panel finance-list"><div className="panel-heading"><div><h2>Order balances</h2><p className="muted">Every project reconciled against payments linked to it</p></div><span className="connection-count">{projects.length}</span></div>{projects.length ? <div className="rows">{projects.map((project) => { const received = projectReceived(project.id, transactions); const due = projectBalance(project, transactions); const ratio = Number(project.budget) > 0 ? Math.min(100, Math.round((received / Number(project.budget)) * 100)) : 0; return <div className="data-row" key={project.id}><div className="row-main"><div className="project-dot">◇</div><div><strong>{project.title}</strong><small>{clientName(project.clientId)} · {money(received)} received of {money(project.budget || 0)}</small><div className="progress-track compact"><span style={{ width: `${ratio}%` }} /></div></div></div><div className="row-right balance-right"><span className={`status ${due === 0 && Number(project.budget) > 0 ? "complete" : received > 0 ? "active" : "on-hold"}`}>{due === 0 && Number(project.budget) > 0 ? "Paid" : received > 0 ? "Partially paid" : "Unpaid"}</span><strong>{money(due)} due</strong></div></div>; })}</div> : <Empty title="No orders yet" text="Create a project with its full order value to start a live balance." />}</section><section className="panel list-panel finance-list"><div className="panel-heading"><div><h2>Payment and expense history</h2><p className="muted">Sample payments, advances, final payments, and business costs</p></div><span className="connection-count">{transactions.length}</span></div>{transactions.length ? <div className="rows">{[...transactions].sort((a, b) => String(b.date || b.createdAt).localeCompare(String(a.date || a.createdAt))).map((item) => <div className="data-row" key={item.id}><div className="row-main"><div className={`money-mark ${item.transactionType === "income" ? "income" : "expense"}`}>{item.transactionType === "income" ? "↑" : "↓"}</div><div><strong>{item.title}</strong><small>{stageLabel(item)} · {item.date || dateLabel(item.createdAt)} · {projectName(item.projectId)} · {clientName(item.clientId)}</small></div></div><div className="row-right"><strong className={item.transactionType === "income" ? "money-positive" : "money-negative"}>{item.transactionType === "income" ? "+" : "−"}{money(item.amount || 0)}</strong><button className="text-button" onClick={() => edit(item)}>Edit</button><button className="text-button danger" onClick={() => remove(item.id)}>Remove</button></div></div>)}</div> : <Empty title="No payments or expenses yet" text="Add a sample payment, an advance, a final payment, or a business expense." />}</section></div>;
}

function RecordDetail({ item, clients, projects, files, clientName, projectName, onBack, onSelect, onPreview, onAddFile, onAddProject }: { item: RecordItem; clients: RecordItem[]; projects: RecordItem[]; files: RecordItem[]; clientName: (id?: string) => string; projectName: (id?: string) => string; onBack: () => void; onSelect: (item: RecordItem) => void; onPreview: (id: string) => void; onAddFile: (projectId: string) => void; onAddProject: (clientId: string) => void }) {
  const linkedProjects = item.kind === "client" ? projects.filter((project) => project.clientId === item.id) : [];
  const linkedFiles = item.kind === "project" ? files.filter((file) => file.projectId === item.id) : item.kind === "client" ? files.filter((file) => linkedProjects.some((project) => project.id === file.projectId)) : [];
  return <div className="page detail-page"><button className="back-link" onClick={onBack}>← Back to {item.kind === "client" ? "clients" : item.kind === "project" ? "projects" : item.kind === "file" ? "files" : "accounts"}</button><div className="detail-heading"><div className="detail-title"><div className={item.kind === "file" ? "detail-symbol file-icon" : "detail-symbol"}>{item.kind === "file" ? (item.mimeType?.startsWith("image/") ? "▧" : "▤") : item.title.slice(0, 1).toUpperCase()}</div><div><p className="eyebrow">{item.kind} record</p><h1>{item.title}</h1><p className="muted">Added {dateLabel(item.createdAt)}</p></div></div><div className="detail-actions">{item.kind === "client" ? <button className="primary" onClick={() => onAddProject(item.id)}>＋ New project</button> : null}{item.kind === "project" ? <button className="primary" onClick={() => onAddFile(item.id)}>＋ Add file</button> : null}{item.kind === "file" ? <button className="primary" onClick={() => onPreview(item.id)}>Preview</button> : null}</div></div><div className="detail-grid"><section className="panel detail-card"><h2>Details</h2>{item.kind === "client" ? <><DetailLine label="Company" value={item.company || "Not added"} /><DetailLine label="Email" value={item.email || "Not added"} /><DetailLine label="Phone" value={item.phone || "Not added"} /><DetailLine label="Notes" value={item.notes || "No notes yet"} /></> : null}{item.kind === "project" ? <><DetailLine label="Client" value={clientName(item.clientId)} onClick={() => { const client = clients.find((candidate) => candidate.id === item.clientId); if (client) onSelect(client); }} /><DetailLine label="Status" value={item.status || "Planning"} /><DetailLine label="Budget" value={money(item.budget || 0)} /><DetailLine label="Progress" value={`${item.progress || 0}%`} /><div className="progress-track"><span style={{ width: `${Math.min(100, Math.max(0, item.progress || 0))}%` }} /></div><DetailLine label="Deadline" value={item.deadline ? dateLabel(item.deadline) : "Not set"} /><DetailLine label="Notes" value={item.notes || "No notes yet"} /></> : null}{item.kind === "file" ? <><DetailLine label="Project" value={projectName(item.projectId)} /><DetailLine label="Type" value={item.mimeType || "Unknown file"} /><DetailLine label="Size" value={bytes(item.size)} /><DetailLine label="Added" value={dateLabel(item.createdAt)} /></> : null}{item.kind === "account" ? <><DetailLine label="Email" value={item.email || "Not added"} /><DetailLine label="Role" value={item.role || "Member"} /><DetailLine label="Status" value={item.status || "Active"} /></> : null}</section>{item.kind === "file" ? <section className="panel inline-preview"><h2>Preview</h2><FilePreview file={item} /><button className="secondary preview-button" onClick={() => onPreview(item.id)}>Open full preview</button></section> : <section className="panel connected-card"><div className="panel-heading"><div><h2>{item.kind === "client" ? "Connected projects" : "Attached files"}</h2><p className="muted">{item.kind === "client" ? "Work linked to this client" : "References stored with this project"}</p></div><span className="connection-count">{item.kind === "client" ? linkedProjects.length : linkedFiles.length}</span></div>{item.kind === "client" ? linkedProjects.length ? <div className="connected-list">{linkedProjects.map((project) => <button key={project.id} onClick={() => onSelect(project)}><span className="project-dot">◇</span><span><strong>{project.title}</strong><small>{project.status || "Planning"} · {money(project.budget || 0)} · {project.progress || 0}%</small></span><em>→</em></button>)}</div> : <Empty title="No projects linked" text="Create a project and select this client." /> : linkedFiles.length ? <div className="connected-list">{linkedFiles.map((file) => <button key={file.id} onClick={() => onPreview(file.id)}><span className="file-icon">{file.mimeType?.startsWith("image/") ? "▧" : "▤"}</span><span><strong>{file.fileName || file.title}</strong><small>{bytes(file.size)}</small></span><em>Preview →</em></button>)}</div> : <Empty title="No files attached" text="Add a file from this project." />}</section>}</div></div>;
}

function RecordDetailV2({ item, clients, projects, files, transactions, clientName, projectName, onBack, onSelect, onEdit, onPreview, onAddFile, onAddProject }: { item: RecordItem; clients: RecordItem[]; projects: RecordItem[]; files: RecordItem[]; transactions: RecordItem[]; clientName: (id?: string) => string; projectName: (id?: string) => string; onBack: () => void; onSelect: (item: RecordItem) => void; onEdit: (item: RecordItem) => void; onPreview: (id: string) => void; onAddFile: (projectId: string) => void; onAddProject: (clientId: string) => void }) {
  const linkedProjects = item.kind === "client" ? projects.filter((project) => project.clientId === item.id) : [];
  const linkedFiles = item.kind === "project" ? files.filter((file) => file.projectId === item.id) : item.kind === "client" ? files.filter((file) => linkedProjects.some((project) => project.id === file.projectId)) : [];
  const received = item.kind === "project" ? projectReceived(item.id, transactions) : 0;
  const due = item.kind === "project" ? projectBalance(item, transactions) : 0;
  return <div className="page detail-page"><button className="back-link" onClick={onBack}>← Back to {item.kind === "client" ? "clients" : item.kind === "project" ? "projects" : item.kind === "file" ? "files" : "accounts"}</button><div className="detail-heading"><div className="detail-title"><div className={item.kind === "file" ? "detail-symbol file-icon" : "detail-symbol"}>{item.kind === "file" ? (item.mimeType?.startsWith("image/") ? "▧" : "▤") : item.title.slice(0, 1).toUpperCase()}</div><div><p className="eyebrow">{item.kind} record</p><h1>{item.title}</h1><p className="muted">Added {dateLabel(item.createdAt)}</p></div></div><div className="detail-actions">{item.kind === "project" ? <button className="secondary" onClick={() => onEdit(item)}>Edit project</button> : null}{item.kind === "client" ? <><button className="secondary" onClick={() => onEdit(item)}>Edit client</button><button className="primary" onClick={() => onAddProject(item.id)}>＋ New project</button></> : null}{item.kind === "project" ? <button className="primary" onClick={() => onAddFile(item.id)}>＋ Add file</button> : null}{item.kind === "file" ? <button className="primary" onClick={() => onPreview(item.id)}>Preview</button> : null}{item.kind === "account" ? <button className="secondary" onClick={() => onEdit(item)}>Edit account</button> : null}</div></div><div className="detail-grid"><section className="panel detail-card"><h2>Details</h2>{item.kind === "client" ? <><DetailLine label="Company" value={item.company || "Not added"} /><DetailLine label="Email" value={item.email || "Not added"} /><DetailLine label="Phone" value={item.phone || "Not added"} /><DetailLine label="Notes" value={item.notes || "No notes yet"} /></> : null}{item.kind === "project" ? <><DetailLine label="Client" value={clientName(item.clientId)} onClick={() => { const client = clients.find((candidate) => candidate.id === item.clientId); if (client) onSelect(client); }} /><DetailLine label="Status" value={item.status || "Planning"} /><DetailLine label="Order total" value={money(item.budget || 0)} /><DetailLine label="Customer payments" value={money(received)} /><DetailLine label="Balance due" value={money(due)} /><DetailLine label="Progress" value={`${item.progress || 0}%`} /><div className="progress-track"><span style={{ width: `${Math.min(100, Math.max(0, item.progress || 0))}%` }} /></div><DetailLine label="Deadline" value={item.deadline ? dateLabel(item.deadline) : "Not set"} /><DetailLine label="Notes" value={item.notes || "No notes yet"} /></> : null}{item.kind === "file" ? <><DetailLine label="Project" value={projectName(item.projectId)} /><DetailLine label="Type" value={item.mimeType || "Unknown file"} /><DetailLine label="Size" value={bytes(item.size)} /><DetailLine label="Added" value={dateLabel(item.createdAt)} /></> : null}{item.kind === "account" ? <><DetailLine label="Email" value={item.email || "Not added"} /><DetailLine label="Role" value={item.role || "Member"} /><DetailLine label="Status" value={item.status || "Active"} /></> : null}</section>{item.kind === "file" ? <section className="panel inline-preview"><h2>Preview</h2><FilePreview file={item} /><button className="secondary preview-button" onClick={() => onPreview(item.id)}>Open full preview</button></section> : <section className="panel connected-card"><div className="panel-heading"><div><h2>{item.kind === "client" ? "Connected projects" : "Attached files"}</h2><p className="muted">{item.kind === "client" ? "Work linked to this client" : "References stored with this project"}</p></div><span className="connection-count">{item.kind === "client" ? linkedProjects.length : linkedFiles.length}</span></div>{item.kind === "client" ? linkedProjects.length ? <div className="connected-list">{linkedProjects.map((project) => <button key={project.id} onClick={() => onSelect(project)}><span className="project-dot">◇</span><span><strong>{project.title}</strong><small>{project.status || "Planning"} · {money(project.budget || 0)} · {money(projectBalance(project, transactions))} due · {project.progress || 0}%</small></span><em>→</em></button>)}</div> : <Empty title="No projects linked" text="Create a project and select this client." /> : linkedFiles.length ? <div className="connected-list">{linkedFiles.map((file) => <button key={file.id} onClick={() => onPreview(file.id)}><span className="file-icon">{file.mimeType?.startsWith("image/") ? "▧" : "▤"}</span><span><strong>{file.fileName || file.title}</strong><small>{bytes(file.size)}</small></span><em>Preview →</em></button>)}</div> : <Empty title="No files attached" text="Add a file from this project." />}</section>}</div></div>;
}

function DetailLine({ label, value, onClick }: { label: string; value: string; onClick?: () => void }) { return <div className="detail-line"><span>{label}</span>{onClick && value !== "Unassigned" ? <button onClick={onClick}>{value} →</button> : <strong>{value}</strong>}</div>; }
function FilePreview({ file }: { file: RecordItem }) { if (!file.dataUrl) return <div className="preview-placeholder"><span className="file-icon">▤</span><strong>Preview unavailable</strong><small>Download the original file to open it.</small></div>; if (isPicture(file)) return <img className="preview-image" src={file.dataUrl} alt={file.fileName || file.title} />; if (file.mimeType === "application/pdf") return <iframe className="preview-frame" src={file.dataUrl} title={file.fileName || file.title} />; return <div className="preview-placeholder"><span className="file-icon">▤</span><strong>{file.fileName || file.title}</strong><small>This file type is ready to download.</small></div>; }
function PreviewModal({ file, projectName, onClose }: { file: RecordItem; projectName: (id?: string) => string; onClose: () => void }) { return <div className="modal-backdrop" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><section className="preview-modal"><div className="modal-heading"><div><p className="eyebrow">File preview</p><h2>{file.fileName || file.title}</h2><p className="muted">{projectName(file.projectId)} · {bytes(file.size)}</p></div><button className="close" onClick={onClose}>×</button></div><div className="preview-stage"><FilePreview file={file} /></div><div className="modal-actions"><button className="secondary" onClick={onClose}>Close</button>{file.dataUrl ? <a className="primary download-button" href={file.dataUrl} download={file.fileName || file.title}>Download</a> : null}</div></section></div>; }

function Settings() { return <div className="page"><div className="page-heading"><div><p className="eyebrow">Workspace</p><h1>Settings</h1><p className="muted">Keep the workspace intentional and easy to use.</p></div></div><section className="panel settings-card"><div><span className="settings-icon">⚙</span><div><h2>Simple workspace mode</h2><p className="muted">Aphuranta now focuses on the records you asked for. Dashboard numbers are derived from saved clients, projects, files, and accounts—there are no demo amounts or placeholder counts.</p></div></div><div className="setting-line"><span>Storage</span><strong>MongoDB workspace</strong></div><div className="setting-line"><span>Image and file limit</span><strong>4 MB per attachment</strong></div></section></div>; }

function CreateModal({ kind, clients, projects, initialProjectId, initialClientId, onClose, onSave }: { kind: Kind; clients: RecordItem[]; projects: RecordItem[]; initialProjectId?: string; initialClientId?: string; onClose: () => void; onSave: (record: RecordItem) => void }) {
  const [values, setValues] = useState<Record<string, string>>({ status: "Planning", role: "Member", accountStatus: "Active", projectId: initialProjectId || "", clientId: initialClientId || "" });
  const [file, setFile] = useState<File | null>(null);
  const [error, setError] = useState("");
  const set = (key: string, value: string) => setValues((current) => ({ ...current, [key]: value }));
  const onFile = (event: ChangeEvent<HTMLInputElement>) => { const selected = event.target.files?.[0]; if (!selected) return; if (selected.size > 4 * 1024 * 1024) { setError("Please choose a file smaller than 4 MB."); return; } setError(""); setFile(selected); };
  const submit = (event: FormEvent) => { event.preventDefault(); const title = kind === "file" ? file?.name : values.name?.trim(); if (!title) { setError(kind === "file" ? "Choose a file first." : "Add a name first."); return; } const base: RecordItem = { id: makeId(kind), kind, title, createdAt: new Date().toISOString() }; if (kind === "client") onSave({ ...base, company: values.company, email: values.email, phone: values.phone, notes: values.notes }); if (kind === "project") onSave({ ...base, clientId: values.clientId || undefined, status: values.status, budget: Number(values.budget) || 0, progress: Math.min(100, Math.max(0, Number(values.progress) || 0)), deadline: values.deadline, notes: values.notes }); if (kind === "account") onSave({ ...base, email: values.email, role: values.role, status: values.accountStatus }); if (kind === "transaction") onSave({ ...base, title: values.name?.trim() || (values.type === "income" ? "Income" : "Expense"), amount: Number(values.amount) || 0, transactionType: values.type === "expense" ? "expense" : "income", category: values.category, date: values.date || new Date().toISOString().slice(0, 10), projectId: values.projectId || undefined, clientId: values.clientId || undefined, notes: values.notes }); if (kind === "file" && file) { const reader = new FileReader(); reader.onload = () => onSave({ ...base, fileName: file.name, mimeType: file.type, size: file.size, dataUrl: typeof reader.result === "string" ? reader.result : undefined, projectId: values.projectId || undefined }); reader.readAsDataURL(file); } };
  const labels: Record<Kind, string> = { client: "New client", project: "New project", file: "Add file or picture", account: "Add team account", transaction: "Add transaction" };
  return <div className="modal-backdrop" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><form className="modal" onSubmit={submit}><div className="modal-heading"><div><p className="eyebrow">Add to workspace</p><h2>{labels[kind]}</h2></div><button type="button" className="close" onClick={onClose}>×</button></div>{kind === "client" ? <><Field label="Full name" value={values.name || ""} onChange={(value) => set("name", value)} placeholder="e.g. Amina Rahman" required /><div className="form-grid"><Field label="Company" value={values.company || ""} onChange={(value) => set("company", value)} placeholder="Company name" /><Field label="Phone" value={values.phone || ""} onChange={(value) => set("phone", value)} placeholder="+880…" /></div><Field label="Email" value={values.email || ""} onChange={(value) => set("email", value)} placeholder="name@company.com" type="email" /><TextArea label="Notes" value={values.notes || ""} onChange={(value) => set("notes", value)} placeholder="Anything useful to remember" /></> : null}{kind === "project" ? <><Field label="Project name" value={values.name || ""} onChange={(value) => set("name", value)} placeholder="e.g. Winter collection" required /><div className="form-grid"><Select label="Client" value={values.clientId || ""} onChange={(value) => set("clientId", value)} options={[{ label: "Unassigned", value: "" }, ...clients.map((client) => ({ label: client.title, value: client.id }))]} /><Select label="Status" value={values.status || "Planning"} onChange={(value) => set("status", value)} options={statuses.map((status) => ({ label: status, value: status }))} /></div><div className="form-grid"><Field label="Budget (৳)" value={values.budget || ""} onChange={(value) => set("budget", value)} placeholder="0" type="number" min="0" /><Field label="Progress %" value={values.progress || ""} onChange={(value) => set("progress", value)} placeholder="0" type="number" min="0" /></div><Field label="Deadline" value={values.deadline || ""} onChange={(value) => set("deadline", value)} type="date" /><TextArea label="Notes" value={values.notes || ""} onChange={(value) => set("notes", value)} placeholder="What needs to be done?" /></> : null}{kind === "transaction" ? <><Field label="Description" value={values.name || ""} onChange={(value) => set("name", value)} placeholder="e.g. Client advance" required /><div className="form-grid"><Select label="Type" value={values.type || "income"} onChange={(value) => set("type", value)} options={[{ label: "Income", value: "income" }, { label: "Expense", value: "expense" }]} /><Field label="Amount (৳)" value={values.amount || ""} onChange={(value) => set("amount", value)} placeholder="0" type="number" min="0" required /></div><div className="form-grid"><Field label="Category" value={values.category || ""} onChange={(value) => set("category", value)} placeholder="Materials, payroll…" /><Field label="Date" value={values.date || ""} onChange={(value) => set("date", value)} type="date" /></div><div className="form-grid"><Select label="Client" value={values.clientId || ""} onChange={(value) => set("clientId", value)} options={[{ label: "No client", value: "" }, ...clients.map((client) => ({ label: client.title, value: client.id }))]} /><Select label="Project" value={values.projectId || ""} onChange={(value) => set("projectId", value)} options={[{ label: "No project", value: "" }, ...projects.map((project) => ({ label: project.title, value: project.id }))]} /></div><TextArea label="Notes" value={values.notes || ""} onChange={(value) => set("notes", value)} placeholder="Optional context" /></> : null}{kind === "account" ? <><Field label="Name" value={values.name || ""} onChange={(value) => set("name", value)} placeholder="Team member name" required /><Field label="Email" value={values.email || ""} onChange={(value) => set("email", value)} placeholder="name@aphuranta.com" type="email" required /><div className="form-grid"><Select label="Role" value={values.role || "Member"} onChange={(value) => set("role", value)} options={["Administrator", "Manager", "Member"].map((role) => ({ label: role, value: role }))} /><Select label="Status" value={values.accountStatus || "Active"} onChange={(value) => set("accountStatus", value)} options={["Active", "Invited", "Paused"].map((status) => ({ label: status, value: status }))} /></div></> : null}{kind === "file" ? <><label className="upload-box"><input type="file" accept="image/*,.pdf,.doc,.docx,.xls,.xlsx,.csv,.zip" onChange={onFile} /><span className="upload-icon">⌁</span><strong>{file ? file.name : "Choose a file or picture"}</strong><small>{file ? bytes(file.size) : "Images, tech packs, PDFs, and documents up to 4 MB"}</small></label><Select label="Attach to project" value={values.projectId || ""} onChange={(value) => set("projectId", value)} options={[{ label: "No project", value: "" }, ...projects.map((project) => ({ label: project.title, value: project.id }))]} /></> : null}{error ? <p className="form-error">{error}</p> : null}<div className="modal-actions"><button type="button" className="secondary" onClick={onClose}>Cancel</button><button type="submit" className="primary">Save {kind === "file" ? "file" : kind}</button></div></form></div>;
}

function CreateModalV2({ kind, initial, clients, projects, initialProjectId, initialClientId, onClose, onSave }: { kind: Kind; initial?: RecordItem; clients: RecordItem[]; projects: RecordItem[]; initialProjectId?: string; initialClientId?: string; onClose: () => void; onSave: (record: RecordItem) => void }) {
  const [values, setValues] = useState<Record<string, string>>({
    name: initial?.title || "",
    company: initial?.company || "",
    email: initial?.email || "",
    phone: initial?.phone || "",
    notes: initial?.notes || "",
    status: initial?.status || "Planning",
    budget: initial?.budget !== undefined ? String(initial.budget) : "",
    progress: initial?.progress !== undefined ? String(initial.progress) : "",
    deadline: initial?.deadline || "",
    role: initial?.role || "Member",
    accountStatus: initial?.status || "Active",
    type: initial?.transactionType || "income",
    accountingType: initial?.accountingType || (initial?.transactionType === "expense" ? "expense" : "customer-payment"),
    paymentStage: initial?.paymentStage || "other",
    amount: initial?.amount !== undefined ? String(initial.amount) : "",
    category: initial?.category || "",
    date: initial?.date || new Date().toISOString().slice(0, 10),
    paymentMethod: initial?.paymentMethod || "",
    projectId: initial?.projectId || initialProjectId || "",
    clientId: initial?.clientId || initialClientId || "",
  });
  const [file, setFile] = useState<File | null>(null);
  const [error, setError] = useState("");
  const set = (key: string, value: string) => setValues((current) => ({ ...current, [key]: value }));
  const onFile = (event: ChangeEvent<HTMLInputElement>) => { const selected = event.target.files?.[0]; if (!selected) return; if (selected.size > 4 * 1024 * 1024) { setError("Please choose a file smaller than 4 MB."); return; } setError(""); setFile(selected); };
  const submit = (event: FormEvent) => {
    event.preventDefault();
    const title = kind === "file" ? file?.name : values.name?.trim();
    if (!title) { setError(kind === "file" ? "Choose a file first." : "Add a name first."); return; }
    if (kind === "transaction" && Number(values.amount) <= 0) { setError("Enter an amount greater than zero."); return; }
    const base: RecordItem = { id: initial?.id || makeId(kind), kind, title, createdAt: initial?.createdAt || new Date().toISOString() };
    if (kind === "client") onSave({ ...base, company: values.company, email: values.email, phone: values.phone, notes: values.notes });
    if (kind === "project") onSave({ ...base, clientId: values.clientId || undefined, status: values.status, budget: Number(values.budget) || 0, progress: Math.min(100, Math.max(0, Number(values.progress) || 0)), deadline: values.deadline || undefined, notes: values.notes });
    if (kind === "account") onSave({ ...base, email: values.email, role: values.role, status: values.accountStatus });
    if (kind === "transaction") onSave({ ...base, title: values.name?.trim() || (values.type === "income" ? "Income" : "Expense"), amount: Number(values.amount) || 0, transactionType: values.type === "expense" ? "expense" : "income", accountingType: values.type === "expense" ? "expense" : (values.accountingType as AccountingType), paymentStage: values.type === "expense" ? undefined : (values.paymentStage as PaymentStage), category: values.category || undefined, date: values.date || new Date().toISOString().slice(0, 10), paymentMethod: values.paymentMethod || undefined, projectId: values.projectId || undefined, clientId: values.clientId || undefined, notes: values.notes });
    if (kind === "file" && file) { const reader = new FileReader(); reader.onload = () => onSave({ ...base, fileName: file.name, mimeType: file.type, size: file.size, dataUrl: typeof reader.result === "string" ? reader.result : undefined, projectId: values.projectId || undefined }); reader.readAsDataURL(file); }
  };
  const labels: Record<Kind, string> = { client: initial ? "Edit client" : "New client", project: initial ? "Edit project" : "New project", file: "Add file or picture", account: initial ? "Edit team account" : "Add team account", transaction: initial ? "Edit payment or expense" : "Add payment or expense" };
  return <div className="modal-backdrop" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><form className="modal" onSubmit={submit}><div className="modal-heading"><div><p className="eyebrow">{initial ? "Update workspace record" : "Add to workspace"}</p><h2>{labels[kind]}</h2></div><button type="button" className="close" onClick={onClose}>×</button></div>{kind === "client" ? <><Field label="Full name" value={values.name || ""} onChange={(value) => set("name", value)} placeholder="e.g. Amina Rahman" required /><div className="form-grid"><Field label="Company" value={values.company || ""} onChange={(value) => set("company", value)} placeholder="Company name" /><Field label="Phone" value={values.phone || ""} onChange={(value) => set("phone", value)} placeholder="+880…" /></div><Field label="Email" value={values.email || ""} onChange={(value) => set("email", value)} placeholder="name@company.com" type="email" /><TextArea label="Notes" value={values.notes || ""} onChange={(value) => set("notes", value)} placeholder="Anything useful to remember" /></> : null}{kind === "project" ? <><Field label="Project / order name" value={values.name || ""} onChange={(value) => set("name", value)} placeholder="e.g. Winter collection — 1 lac order" required /><div className="form-grid"><Select label="Client" value={values.clientId || ""} onChange={(value) => set("clientId", value)} options={[{ label: "Unassigned", value: "" }, ...clients.map((client) => ({ label: client.title, value: client.id }))]} /><Select label="Status" value={values.status || "Planning"} onChange={(value) => set("status", value)} options={statuses.map((status) => ({ label: status, value: status }))} /></div><div className="form-grid"><Field label="Full order value (৳)" value={values.budget || ""} onChange={(value) => set("budget", value)} placeholder="100000" type="number" min="0" /><Field label="Work progress %" value={values.progress || ""} onChange={(value) => set("progress", value)} placeholder="0" type="number" min="0" /></div><Field label="Deadline" value={values.deadline || ""} onChange={(value) => set("deadline", value)} type="date" /><TextArea label="Notes" value={values.notes || ""} onChange={(value) => set("notes", value)} placeholder="What needs to be done?" /></> : null}{kind === "transaction" ? <><Field label="Description" value={values.name || ""} onChange={(value) => set("name", value)} placeholder="e.g. Client advance — order #001" required /><div className="form-grid"><Select label="Type" value={values.type || "income"} onChange={(value) => set("type", value)} options={[{ label: "Money received", value: "income" }, { label: "Business expense", value: "expense" }]} /><Field label="Amount (৳)" value={values.amount || ""} onChange={(value) => set("amount", value)} placeholder="2000" type="number" min="0" required /></div>{values.type !== "expense" ? <div className="form-grid"><Select label="Accounting treatment" value={values.accountingType || "customer-payment"} onChange={(value) => set("accountingType", value)} options={[{ label: "Customer payment", value: "customer-payment" }, { label: "Other income", value: "other-income" }]} />{values.accountingType === "customer-payment" ? <Select label="Payment stage" value={values.paymentStage || "other"} onChange={(value) => set("paymentStage", value)} options={paymentStages} /> : <Field label="Income category" value={values.category || ""} onChange={(value) => set("category", value)} placeholder="Other income" />}</div> : null}<div className="form-grid"><Field label={values.type === "expense" ? "Expense category" : "Category"} value={values.category || ""} onChange={(value) => set("category", value)} placeholder={values.type === "expense" ? "Sample making, fabric, payroll…" : "Optional label"} /><Field label="Date" value={values.date || ""} onChange={(value) => set("date", value)} type="date" /></div><div className="form-grid"><Select label="Client" value={values.clientId || ""} onChange={(value) => set("clientId", value)} options={[{ label: "No client", value: "" }, ...clients.map((client) => ({ label: client.title, value: client.id }))]} /><Select label="Project / order" value={values.projectId || ""} onChange={(value) => set("projectId", value)} options={[{ label: "No project", value: "" }, ...projects.map((project) => ({ label: project.title, value: project.id }))]} /></div><Field label="Payment method" value={values.paymentMethod || ""} onChange={(value) => set("paymentMethod", value)} placeholder="Bank, bKash, cash, card…" /><TextArea label="Notes" value={values.notes || ""} onChange={(value) => set("notes", value)} placeholder="Optional receipt or reconciliation note" /></> : null}{kind === "account" ? <><Field label="Name" value={values.name || ""} onChange={(value) => set("name", value)} placeholder="Team member name" required /><Field label="Email" value={values.email || ""} onChange={(value) => set("email", value)} placeholder="name@aphuranta.com" type="email" required /><div className="form-grid"><Select label="Role" value={values.role || "Member"} onChange={(value) => set("role", value)} options={["Administrator", "Manager", "Member"].map((role) => ({ label: role, value: role }))} /><Select label="Status" value={values.accountStatus || "Active"} onChange={(value) => set("accountStatus", value)} options={["Active", "Invited", "Paused"].map((status) => ({ label: status, value: status }))} /></div></> : null}{kind === "file" ? <><label className="upload-box"><input type="file" accept="image/*,.pdf,.doc,.docx,.xls,.xlsx,.csv,.zip" onChange={onFile} /><span className="upload-icon">⌁</span><strong>{file ? file.name : "Choose a file or picture"}</strong><small>{file ? bytes(file.size) : "Images, tech packs, PDFs, and documents up to 4 MB"}</small></label><Select label="Attach to project" value={values.projectId || ""} onChange={(value) => set("projectId", value)} options={[{ label: "No project", value: "" }, ...projects.map((project) => ({ label: project.title, value: project.id }))]} /></> : null}{error ? <p className="form-error">{error}</p> : null}<div className="modal-actions"><button type="button" className="secondary" onClick={onClose}>Cancel</button><button type="submit" className="primary">{initial ? "Save changes" : `Save ${kind === "file" ? "file" : kind}`}</button></div></form></div>;
}

function Field({ label, value, onChange, placeholder, type = "text", required = false, min }: { label: string; value: string; onChange: (value: string) => void; placeholder?: string; type?: string; required?: boolean; min?: string }) { return <label className="field"><span>{label}</span><input required={required} type={type} min={min} value={value} placeholder={placeholder} onChange={(event) => onChange(event.target.value)} /></label>; }
function TextArea({ label, value, onChange, placeholder }: { label: string; value: string; onChange: (value: string) => void; placeholder?: string }) { return <label className="field"><span>{label}</span><textarea value={value} placeholder={placeholder} onChange={(event) => onChange(event.target.value)} rows={3} /></label>; }
function Select({ label, value, onChange, options }: { label: string; value: string; onChange: (value: string) => void; options: { label: string; value: string }[] }) { return <label className="field"><span>{label}</span><select value={value} onChange={(event) => onChange(event.target.value)}>{options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label>; }
